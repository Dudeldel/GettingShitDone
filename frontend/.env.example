# Base URL of the Laravel API. Vite inlines this at BUILD time.
# Two-service deploy on Railway: set this to the backend service's public domain,
#   e.g. https://gsd-api.up.railway.app  (as a Railway BUILD variable on the frontend service).
# Local dev: leave empty to use the dev server proxy / same origin.
VITE_API_BASE_URL=
