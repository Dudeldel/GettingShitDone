# Base URL of the Laravel API.
# Leave empty for same-origin: nginx serves the SPA and proxies /api on the same domain.
# Set to e.g. https://gsd.example.com only if the API lives on a different origin.
VITE_API_BASE_URL=
